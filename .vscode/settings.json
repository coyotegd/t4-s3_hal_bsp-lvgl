{
  "C_Cpp.intelliSenseEngine": "default",
  "idf.openOcdConfigs": [
    "board/esp32s3-builtin.cfg"
  ],
  "idf.customExtraVars": {
    "IDF_TARGET": "esp32s3",
    "OPENOCD_USB_ADAPTER_LOCATION": "5-1"
  },
  "clangd.arguments": [
    "--background-index",
    "--query-driver=**",
    "--compile-commands-dir=${workspaceFolder}/build"
  ],
  "idf.flashType": "UART",
  "idf.buildPath": "${workspaceFolder}/build",
  "idf.currentSetup": "/home/kenneth/esp/v5.5/esp-idf",
  "idf.port": "/dev/ttyACM0"
  // User-specific paths (auto-configured by ESP-IDF extension):
  // "idf.espIdfPath": "auto-detected or set via extension"
  // "idf.toolsPath": "auto-detected or set via extension"
  // "idf.pythonInstallPath": "auto-detected or set via extension"
  // "idf.port": "select your port when flashing"
  // "clangd.path": "auto-detected from toolsPath"
}
